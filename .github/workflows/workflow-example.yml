name: Test CodeBuild Runner

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test:
    # Use CodeBuild runner
    # Replace 'codebuild-runner-runner' with your actual CodeBuild project name
    runs-on:
      - self-hosted
      - codebuild-codebuild-runner-runner-${{ github.run_id }}-${{ github.run_attempt }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Test runner environment
        run: |
          echo "Running on CodeBuild runner"
          echo "Runner name: ${{ runner.name }}"
          echo "OS: ${{ runner.os }}"
          echo "Architecture: ${{ runner.arch }}"

      - name: Check installed tools
        run: |
          echo "=== Node.js ==="
          node --version
          npm --version

          echo "=== Python ==="
          python3 --version
          pip3 --version

          echo "=== Java ==="
          java -version
          mvn --version

          echo "=== AWS CLI ==="
          aws --version

          echo "=== Git ==="
          git --version

      - name: Test Kaniko
        run: |
          echo "Testing Kaniko availability"
          /kaniko/executor --version || echo "Kaniko executor found"

      - name: Test AWS credentials
        run: |
          echo "Testing AWS credentials"
          aws sts get-caller-identity || echo "AWS credentials not configured (this is OK for testing)"

      - name: Create test file
        run: |
          echo "Hello from CodeBuild runner!" > test.txt
          cat test.txt

      - name: Cleanup
        if: always()
        run: |
          rm -f test.txt

